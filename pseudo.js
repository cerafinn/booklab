// create a backend
// with user keys and an admin


// create frontend where:
// the admin view will see who clocked in and out of the lab
// the users can clock in/out of the lab, can see their own clock times, get reminders checking if they are still at the lab
// time sheets for users and admin, which will count up in time for the week, along with a total for the week